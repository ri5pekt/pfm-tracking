# Examples

Real (or representative) API payloads for fixtures and client development.

## ShipBob

| File | What |
| :--- | :--- |
| `shipbob/order-with-shipment.json` | Synthetic order shape (PII is fake) |
| `shipbob/tracking-delivered.json` | Live Tracking API — Delivered |
| `shipbob/tracking-in-transit.json` | Live Tracking API — InTransit + geo |
| `shipbob/tracking-out-for-delivery.json` | Live Tracking API — OutForDelivery |
| `shipbob/tracking-with-exception-history.json` | Live — history includes exception/attempt |

## TrackingMore

| File | What |
| :--- | :--- |
| `trackingmore/tracking-get-response.json` | Representative GET tracking shape |

## Analysis summaries

CSV/JSON aggregates from research pulls (status frequencies, exception reasons).  
Full raw 5-day JSONL stays in the research repo (large); not required to start coding.
