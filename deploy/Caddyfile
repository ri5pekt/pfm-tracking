{
	admin off
	email admin@particleformen.com
}

tracking.particleformen.com {
	encode gzip
	reverse_proxy admin:80

	header {
		X-Content-Type-Options nosniff
		Referrer-Policy strict-origin-when-cross-origin
		X-Frame-Options SAMEORIGIN
	}

	log {
		output stdout
		format console
	}
}
